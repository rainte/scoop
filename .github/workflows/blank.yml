name: Windows
on:
  workflow_dispatch:
  schedule:
    - cron: "*/1 * * * *"
jobs:
  run:
    name: Run
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@main
      - name: Run
        run: '"Hello, world!" | Add-Content /test.txt'
